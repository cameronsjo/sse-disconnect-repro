[project]
name = "sse-disconnect-repro"
version = "0.1.0"
description = "Reproduction case for FastMCP SSE ASGI protocol violation"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=0.5.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
]
